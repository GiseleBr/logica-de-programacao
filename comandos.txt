
cd //trocar
ls //listar
ipconfig
ping
ping localhost ou 127.0.0.1

cd : //retorna
md //criar
rn //deletar
http://
mv // renomear

10/08

o senao se() é usado quando uma condição tem que ser preenchida
da mesma forma que o se()
já o senao{
}
é usado quando ele não consegue atender nenhuma condição do se() ou senao se()
por isso o senao fica no final e não usa parenteses

VPN = virtual ... network

11/08
git config --global user.name "GiseleBr"
git config --global user.name "britto.gisele@gmail.com"
